# Runtime dependencies. `pip install -e .` reads them from pyproject.toml,
# so this file is only here for the rare case of `pip install -r requirements.txt`.
click>=8.1.7
requests>=2.32.0
rich>=13.7.1
python-dateutil>=2.9.0


to run the code :
cd ~/non-ticketed-activity-tracker
source .venv/bin/activate && python server.py
[or]
python server.py